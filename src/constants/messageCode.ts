/**
 * Enum representing various error codes that can be returned by the system.
 */
export enum ErrorCode {
  NOT_FOUND = "NOT_FOUND",
  INTERNAL_SERVER_ERROR = "INTERNAL_SERVER_ERROR",
  FORBIDDEN = "FORBIDDEN",
}

/**
 * Enum representing various success codes that can be returned by the system.
 */

export enum SuccessCode {
  REGISTERED_SUCCESSFULLY = "REGISTERED_SUCCESSFULLY",
  LOGS_FETCHED_SUCCESSFULLY = "LOGS_FETCHED_SUCCESSFULLY",
}
